// crie um JSON com 3 propriedades;
// atribua ele a uma variavel
// acesse as propriedades imprimindo no console

const obj = {
    "nome": "allison",
    "idade": 16,
    "namorada": "suimdara"
}

console.log(obj.nome);
console.log(obj.idade);
console.log(obj.namorada);